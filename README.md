# chrome-tab-renamer
Rename any browser tab to something actually useful. Right-click any page and choose "Rename this tab" to set a custom name that sticks across refreshes. Perfect for apps like Buffer where multiple tabs look identical. Names are stored locally and never leave your browser.
